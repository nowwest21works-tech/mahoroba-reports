# 名古屋・愛知 土地探し沿線ガイド v0.1

## 目的

まほろば不動産の土地探し相談で使う、沿線・駅別の公開エリアガイドPreview。

## データソース

Notion「愛知県路線DB」「愛知県駅DB」を正本とする。

今回のPreviewでは、Notion MCPで以下を抽出した。

- 路線DB: 「対象区間」が空でない、かつ対象7路線
- 駅DB: 「公開ステータス = 進行中」、かつ「愛知県路線DB」リレーションが対象7路線に紐づく駅

駅名だけでは抽出していない。同名駅混入を避けるため、駅DBの「愛知県路線DB」リレーションで判定している。

## 現在の状態

Preview v0.1。

HTML掲載可はまだ正式公開判定には使っていない。将来の本公開モードでは「HTML掲載可 = YES」のみ出力できるよう、JSON側に `status` を保持している。

公開前にソースURL整備が必要。現在、主要ソースURLが空欄または鉄道会社公式URLのみのレコードがある。

## GitHub Pages 想定

GitHub Pages有効化後の想定パス:

```text
/area-guide/aichi-railway-land-guide-v0-1/
```

ただし、Pages設定・公開URLは今西レビュー後に確認。

## 本公開前チェック

- [ ] sourceUrl / 出典表示を整備
- [ ] HTML掲載可 = YES の対象のみで再エクスポート
- [ ] ハザード表現の断定を確認
- [ ] 地価・乗降者数の時点を確認
- [ ] スマホ実機で表示確認
- [ ] 今西レビュー完了

## 更新方法

1. Notion DBを更新
2. `routes.json` / `stations.json` を更新
3. HTMLを再生成または表示確認
4. 今西レビュー後に公開

## レビュー観点

- 路線名・駅名の抽出漏れ
- 同名駅の混入
- ハザード表現の断定過多
- ソースURLの妥当性
- スマホ表示
- お客様に見せたときの分かりやすさ

## 表示確認

ローカルサーバー経由で確認する。

```powershell
python -m http.server 8000
```

その後、以下を開く。

```text
http://localhost:8000/area-guide/aichi-railway-land-guide-v0-1/
```

## 座標・地理マップメモ

- `stations.json` の `latitude` / `longitude` は Notion「愛知県駅DB」を正本として同期する。
- 座標出典は国土数値情報 N02 鉄道データ 2025年度版。対象駅は駅形状 `LineString` の代表点として扱う。
- Leaflet 地図の路線線分は駅座標を順番に結んだ概略線であり、実際の線路形状ではない。
- 名古屋駅の基準点は N02 2025年度版の名古屋駅レコード群から算出した代表点を使う。
- OpenStreetMap タイルを表示するため、地図表示にはネットワーク接続が必要。読み込み不可の場合は既存の SVG 模式路線図を fallback とする。
